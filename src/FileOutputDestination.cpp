#include "FileOutputDestination.h"

// FileOutputDestination is a base class with all methods defined inline in the header.
// This implementation file is intentionally minimal and exists only to satisfy
// the build system's requirements.
//
// All actual functionality is implemented in derived classes:
// - WavFileDestination
// - Mp3FileDestination
// - OpusFileDestination
// - FlacFileDestination
